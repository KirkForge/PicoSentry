apiVersion: v1
kind: Service
metadata:
  name: {{ include "serve.fullname" . }}
  labels:
    {{- include "serve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
    {{- if .Values.metrics.separatePort }}
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
    {{- end }}
  selector:
    {{- include "serve.selectorLabels" . | nindent 4 }}