{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "serve.serviceAccountName" . }}
  labels:
    {{- include "serve.labels" . | nindent 4 }}
{{- end }}