---
name: session-2025-08-05
description: Ponytail debt cleanup, full review, competitive scoring, production hardening
metadata:
  type: project
---

## What was done

### Round 1: Ponytail debt cleanup (4 branches, −144 lines net)
- Deleted unused Protocols: `ConfigProtocol`, `DeterministicResult`, `FindingProtocol`, 4 service Protocols (`ScannerService` etc.)
- Deleted `serve/config/protocols.py` entirely
- Merged `_deterministic_hash_raw` into `deterministic_hash`
- Delegated `diff_scans` to `_core_diff_results` instead of duplicating
- Removed collision-prone `__hash__` overrides on 3 frozen dataclasses

### Round 2: Production hardening (5 branches, +793/−593 net)
- **Dedup core utils**: Extracted `constant_time_compare` → `_core/security.py`, `now_ms` → `_core/time.py`, `validate_config_keys` + `apply_severity_overrides` → `_core/config.py`, NoOp tracers → `_core/tracing.py`. Renamed `Finding` → `SandboxFinding` in sandbox.
- **Security hardening**: Removed `prctl`/`memfd_create`/`io_uring` from seccomp allowlist, blocked security bypasses in production, added env stripping to sandbox direct API path, added auth-disabled warning for non-localhost.
- **Dashboard production**: Changed serve from BETA → STABLE, added proxy-aware rate limiting, readiness health check, production error sanitization.
- **Edge strengthening**: Added `sandbox/SECURITY.md`, `OFFLINE.md`, `docs/ide-plugin-plan.md`, determinism CI step.
- **Test quality**: Removed `assert True`, fixed mtls no-assert test, added SLO failure tests, fixed tautological imports.

### Review scores (before → after estimates)
- Architecture: 5 → 6+ (dedup to `_core`, ~1900 lines of duplication identified)
- Code Quality: 6 → 7+ (shared utils, no more copy-paste)
- Security: 7 → 8+ (seccomp hardened, production guards, env stripping)
- Tests: 7 → 8+ (real assertions, failure paths)
- Dashboard: beta → **stable**
- Competitive edges documented: sandbox (8/10), deployment (9/10), deterministic (unique)

## Unpushed commits

18 commits ahead of `origin/main`. Push after restart:
```
git push origin main
git checkout dev && git merge main
git push origin dev
```

## Remaining work identified but not done

- **Config dedup**: `scan/config.py`, `sandbox/config.py`, `watch/config.py` share ~1900 lines of near-identical patterns (env-var reading, config loading, merge_cli, assert_secure). A shared base in `_core` could collapse to ~600 lines.
- **Three metrics implementations**: `scan/metrics.py`, `serve/services/metrics.py`, `watch/telemetry/metrics.py` reimplement counters/gauges/histograms/Prometheus output.
- **Module-level singletons**: `settings = Settings()`, `db = DatabaseManager()`, `orchestrator = EnhancedOrchestrator()` etc. create import-time side effects.
- **Watch config property proxies**: 180 lines of `threshold_block = property(lambda self: self.prompt_guard.threshold_block)` delegation.
- **Cross-package boundary**: `sandbox/admission/scanner.py` imports from `scan._network` (private module).
- **Dep-confusion rules have 0% recall**: L2-PYPI-DEPC-001 and L2-MAVEN-DEPC-001 score 0% on generated fixtures.
- **SSO/SAML**: No enterprise identity provider integration.
- **IDE plugin**: Planned (`docs/ide-plugin-plan.md`) but not implemented.