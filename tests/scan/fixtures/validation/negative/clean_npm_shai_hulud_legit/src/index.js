// Trivial helper for the bun-runtime-helper package. Runs on both
// Bun and Node.js. Intentionally contains no known campaign
// indicators from any tracked supply-chain attack corpus.
export function hello() {
  return "hi";
}
