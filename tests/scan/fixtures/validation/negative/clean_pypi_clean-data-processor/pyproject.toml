[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "clean-data-processor"
version = "0.1.0"
description = "A clean Python library: clean-data-processor"
requires-python = ">=3.9"
dependencies = []
