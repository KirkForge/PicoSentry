[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "clean-encoding-utils"
version = "0.1.0"
description = "A clean Python library: clean-encoding-utils"
requires-python = ">=3.9"
dependencies = []
