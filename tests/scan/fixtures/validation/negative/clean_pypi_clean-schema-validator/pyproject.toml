[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "clean-schema-validator"
version = "0.1.0"
description = "A clean Python library: clean-schema-validator"
requires-python = ">=3.9"
dependencies = []
