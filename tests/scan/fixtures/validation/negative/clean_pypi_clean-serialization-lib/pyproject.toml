[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "clean-serialization-lib"
version = "0.1.0"
description = "A clean Python library: clean-serialization-lib"
requires-python = ">=3.9"
dependencies = []
