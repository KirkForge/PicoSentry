[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "zzz-marker-pypi-negative"
version = "1.0.0"
description = "Clean PyPI fixture for typosquat FP contract."
license = {text = "MIT"}
dependencies = ["requests>=2.31.0"]
