{
  "label": "positive",
  "description": "OBFS bypass variant: eval() inside a conditional block (fires L2-OBFS-001). The regex matches the literal `eval(` token regardless of surrounding control flow.",
  "expected_rule_ids": ["L2-OBFS-001"]
}
