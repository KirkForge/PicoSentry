{
  "label": "positive",
  "description": "PYPI OBFS bypass variant: eval(compile(...)) pattern (fires L2-PYPI-OBFS-001). The compile() call is the standard library's way to turn a string into a code object before exec/eval.",
  "expected_rule_ids": ["L2-PYPI-OBFS-001"]
}
