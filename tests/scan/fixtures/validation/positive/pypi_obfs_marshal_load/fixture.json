{
  "label": "positive",
  "description": "PYPI OBFS bypass variant: marshal.load() (file form) instead of marshal.loads() (bytes form) (fires L2-PYPI-OBFS-006). The regex matches both load() and loads().",
  "expected_rule_ids": ["L2-PYPI-OBFS-006"]
}
