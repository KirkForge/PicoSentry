{
  "label": "positive",
  "description": "Worm variant: postinstall rewrites its own package.json (fires L2-WORM-001). The writeFileSync(..., package.json) pattern is the canonical Shai-Hulud self-propagation trait — the worm mutates its own manifest mid-install to inject new lifecycle scripts.",
  "expected_rule_ids": ["L2-WORM-001"]
}
